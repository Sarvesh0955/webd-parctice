//define object
let obj = {
    name: "sarvesh",
    age: 12,
};

console.log(obj); 

//access
console.log(obj.name);
console.log(obj["name"]);

//freeze val
Object.freeze(obj);

//function is a object in javascript
//on applying function_name.length return no of input